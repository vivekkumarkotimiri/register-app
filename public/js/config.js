/* ==========================================================
   CONFIGURATION
   The page talks to the backend at this address. Leave this
   as an empty string if the page is served BY the same server
   (the normal setup -- see README.md). Only change this if you
   ever host the frontend separately from the backend.
   ========================================================== */

const API_BASE = "";
